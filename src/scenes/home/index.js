import React from 'react';
import { View } from 'react-native';

import {
    Text,
} from '_atoms';

const home = () => {
    return (
        <View>
            <Text>This is Home Page.</Text>
        </View>
    );
};

export default home;